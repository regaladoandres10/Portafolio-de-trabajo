
let width = window.prompt("What is the width?", 0)

let height = window.prompt("What is the height?", 0)

let length = window.prompt("What is the length?", 0)

let volume = width * height * length
alert("The volume is: " + volume)
